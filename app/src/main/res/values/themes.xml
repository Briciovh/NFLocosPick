<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.NFLocosPick" parent="Theme.AppCompat.Light.NoActionBar" />

    <!--
        Tema temporal del launcher: se muestra entre el arranque del proceso y el
        primer frame de MainActivity. installSplashScreen() lo mantiene visible
        (setKeepOnScreenCondition) mientras se lee el idioma guardado en DataStore
        de forma asíncrona, y lo reemplaza por postSplashScreenTheme al terminar.
    -->
    <style name="Theme.NFLocosPick.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NFLocosPick</item>
    </style>
</resources>