# Copy this file to keystore.properties (gitignored, never commit it) and
# fill in real values. storeFile can be an absolute path or relative to the
# repo root.
storeFile=/absolute/path/to/release.jks
storePassword=changeme
keyAlias=changeme
keyPassword=changeme
